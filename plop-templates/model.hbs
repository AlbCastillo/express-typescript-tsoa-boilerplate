import { Document, Schema, Model, model } from 'mongoose';
import { v4 as uuid } from 'uuid';
import uniqueValidator from 'mongoose-unique-validator';

/**
 * MONGOOSE {{upperCase name}} INTERFACE
 */
export interface I{{pascalCase name}} extends Document {
	name: string;
}

/**
 * MONGOOSE {{upperCase name}} SCHEMA
 */
export const {{pascalCase name}}Schema: Schema = new Schema(
	{
		id: {
			type: String,
			required: true,
			default: () => uuid(),
		},
		name: {
			type: String,
			required: true,
			unique: true,
		},
	},
	{
		timestamps: true,
	},
);

/**
 *	MONGOOSE PLUGINS
 */
{{pascalCase name}}Schema.plugin(uniqueValidator);

/**
 * MOONGOSE {{upperCase name}} MODEL
 */
export const {{pascalCase name}}Model: Model<I{{pascalCase name}}> = model('{{pascalCase name}}', {{pascalCase name}}Schema);
