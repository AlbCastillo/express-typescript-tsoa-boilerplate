import { singleton } from 'tsyringe';
import logger from '../../../logging/winstonLogger';
import { {{pascalCase name}} } from './{{name}}';

export type {{pascalCase name}}CreationParams = Pick<{{pascalCase name}}, 'name'>;

@singleton()
export class {{pascalCase name}}sService {
	/**
	 * !! DEFAULT METHODS
	 */
	public get(id: string): {{pascalCase name}} {
		logger.info(`Getting {{pascalCase name}} with ${id}`);
		return { id, name: `{{pascalCase name}} Name` };
	}

	public create({{name}}CreationParams: {{pascalCase name}}CreationParams): {{pascalCase name}} {
		return {
			id: Math.floor(Math.random() * 10000).toString(), // Random
			...{{name}}CreationParams,
		};
	}
}
