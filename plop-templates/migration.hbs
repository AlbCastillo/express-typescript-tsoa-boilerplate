module.exports = {
	up: (queryInterface, Sequelize) => {
		return queryInterface.createTable('{{name}}s', {
			id: {
				type: Sequelize.UUID,
				defaultValue: Sequelize.UUIDV4,
				allowNull: false,
				primaryKey: true,
			},

			name: {
				type: Sequelize.STRING,
				allowNull: false,
			},
			version: {
				type: Sequelize.INTEGER,
				allowNull: false,
			},
			createdAt: {
				type: Sequelize.DATE,
				allowNull: false,
				defaultValue: new Date(),
			},
			updatedAt: {
				type: Sequelize.DATE,
				allowNull: false,
				defaultValue: new Date(),
			},

			/**
			 * ADD PROPERTIES
			 */
		});
	},
	down: (queryInterface) => {
		return queryInterface.dropTable('{{name}}s');
	},
};
